{
  "name": "valheim-mapper",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "node server/server.js",
    "assets": "node scripts/copy_assets.mjs"
  }
}
