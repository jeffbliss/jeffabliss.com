{
  "name": "psn-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node --env-file-if-exists=.env --import tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "psn-api": "^2.18.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
